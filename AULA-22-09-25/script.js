function soma() {
    let v1 = 200;
    let v2 = 400;
    let total = v1 + v2;

    alert("Total = " + total);
}

function subtracao() {
    let v1 = 200;
    let v2 = 400;
    let total = v1 - v2;

    alert("Total = " + total)
}

function multiplicacao() {
    let v1 = 200; 
    let v2 = 400;
    let total = v1 * v2;

    alert("Total = " + total)
}

function divisao() {
    let v1 = 200;
    let v2 = 400;
    let total = v1 / v2;

    alert("Total = " + total)
}

// É uma boa prática você separar os módulos e importar numa página principal, entra o conceito de clean code e organização para futuras manutenções


//prompt()-- exibe caixas de diálogo- Mensagem: Texto q aparece na janela para orientar o usuário - Valor padrão - Opcional 
// Exemplo: Uma caixa de texto que pede a seguinte informação: "Qual é seu nome? " -- Se o prompt for cancelado,retorna o valor null
